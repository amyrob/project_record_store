<h2>Artists</h2>

<table>
  <tr>
    <th>Artist ID</th>
    <th>Name</th>
  </tr>

  <% @artists.each do |artist| %>

  <tr>
    <td><%= artist.id %></td>
    <td><%= artist.name %></td>
    <td><a href="/artists/<%= artist.id %>" method="get">
       <input type="submit" value="Artist Inventory"></td>
      </tr>
    </form>

<% end %>
</table>
